# PostgreSQL connection string (trust auth -- no password needed for local dev)
DATABASE_URL="postgresql://postgres@localhost:5432/project_indexer_db"
PORT=5000
UPLOAD_PATH=./uploads

# CORS origin (set to your frontend URL in production)
CORS_ORIGIN="*"
